# Environment Variables for Production Deployment

# Session Secret Key (REQUIRED for production)
# Generate a secure random string for session encryption
# Example: use `openssl rand -base64 32` to generate a secure key
SESSION_SECRET=your-secure-session-secret-key-here

# Node Environment (automatically set in vercel.json)
NODE_ENV=production